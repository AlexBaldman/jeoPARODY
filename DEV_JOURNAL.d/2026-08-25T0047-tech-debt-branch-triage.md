# 2026-08-25 00:47 ET — ChatGPT — branch triage and current-runtime debt burn-down

- **Read/inspected:** current `main` and open PRs/issues; every visible non-main branch; merged/superseded feature families; old production-readiness, Devin audit/refactor, CSS/UI, Mac, Sprite Foundry, Qwen image-edit, Stage-motion, Pages, Needle Drop, and multiplayer branches; current dependency/security posture; live runtime imports; old `AUDIT_2026.md`; and exact-head CI evidence for the cleanup branches.
- **Changed:** merged PR #54 as `b4be538` to remove unused Google AI SDKs, `gh-pages`, legacy browser credential paths, and the axe/ChromeDriver dependency tree while upgrading Firebase and adding blocking critical-audit/security contracts; closed stale PRs #35 and #28 with their useful ideas explicitly accounted for; closed completed issues #50/#53 and narrowed #52 to non-critical follow-up; classified branch families in issue #58 as safe retirement, salvage quarry, or intentional park; kept draft Qwen PR #34 parked for later fresh-porting; opened PR #57 to delete `main.js.rej` and the disabled duplicate-authority compatibility bridge; added a production-entrypoint source-reachability inventory; preserved the old `main@e71d0dc` application audit verbatim under `docs/history/`; and split this journal into a small router plus per-entry files while preserving the entire prior monolith verbatim.
- **Evidence/tests:** PR #54 passed critical production/full audits, doctrine/security checks, lint, 94 tests, Vite build, Firebase 12 Firestore emulator, Main/Needle Drop/Head-to-Head browser proofs, accessibility execution, and artifacts before merge. PR #57 exact-head run `32809980618` passed the same wall plus the new reachability audit. That audit reports 52/88 source JS files reachable from the three production app entrypoints and 38 production-unreachable candidates. Main accessibility remains 3 violations, Needle Drop 1, Head-to-Head 0, with fixes tracked by #56. Production dependency audit is currently clean at the critical threshold; the full dev/tool graph has 16 non-critical advisories.
- **Decisions:** do not wholesale-merge divergent historical branches; salvage behavior/assets/provenance onto fresh `main`, then retire branches. Treat production-unreachable code as a candidate, not automatic deletion proof, especially when tests still exercise pure logic. Preserve audits/history exactly while removing their authority over current implementation. Use a small trusted control plane over a large preserved archive. Keep Firebase activation #44 as the product lead domino after this bounded debt pass rather than letting repository gardening become the product.
- **Unresolved:** merge #57 after its final journal-only CI rerun; then use its reachability map to delete one coherent dead family at a time. High-confidence candidates include the unused ConnectedComponent/UI hierarchy and duplicate `services/api.js`; test-only core/scoring/validation and old AI/state families need consolidation decisions rather than blind deletion. Branch deletion itself remains a UI/CLI owner action because the current GitHub connector cannot delete refs. CI also warns that several GitHub Actions target deprecated Node runtimes and `actions/setup-java@v4` should move to v5. Accessibility #56, historical credential revocation #55, non-critical dependency triage #52, and Firebase activation #44 remain open.
- **Next lead domino:** land #57, then take one more coherent dead-source deletion slice using the reachability evidence; certify the safe branch-retirement list in #58; stop the cleanup pass there and return to Firebase #44 for real cloud/two-device multiplayer proof.
- **Refs:** `main@b4be538`, PR #54, PR #57, #34, #44, #52, #55, #56, #58, `scripts/audit-source-reachability.mjs`, `docs/history/AUDIT_2026_E71D0DC.md`.
